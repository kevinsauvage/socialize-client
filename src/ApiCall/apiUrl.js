export const urls = {
  baseUrl:
    window.location.hostname === 'localhost'
      ? 'http://localhost:5000/'
      : 'https://serversocialize.herokuapp.com/',
  login: 'login',
  register: 'users',
}
